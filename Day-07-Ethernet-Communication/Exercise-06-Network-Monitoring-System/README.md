# Exercise 06 - Network Monitoring System

Objective: Simulate devices emitting bandwidth, packet counts, errors, and statuses; aggregate into a dashboard-style output.

Run:

```bash
./scripts/run-ex06.sh
```
