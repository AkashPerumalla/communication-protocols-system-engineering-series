# Exercise 07 - Event Correlation

Correlate power failure and device unreachable events to identify a likely root cause.

## Run
```bash
javac --release 17 -d out ../core/*.java EventCorrelationDemo.java
java -cp out EventCorrelationDemo
```

## Expected Marker
- `ROOT CAUSE`
