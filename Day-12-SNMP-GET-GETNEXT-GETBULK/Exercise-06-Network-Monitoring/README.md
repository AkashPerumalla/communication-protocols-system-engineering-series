# Exercise 06 - Network Monitoring

Poll router, switch, and firewall profiles and compare traditional GET polling with GETBULK retrieval.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') NetworkMonitoringDemo.java
java -cp out NetworkMonitoringDemo
```
