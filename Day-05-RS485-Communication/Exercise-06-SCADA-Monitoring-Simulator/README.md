Exercise 06 — SCADA Monitoring Simulator

Objective
- Simulate a SCADA master polling multiple RTUs and present a simple dashboard showing OK/ALARM states.

Files
- `ScadaMaster.java` — polls RTU devices
- `RemoteUnit.java` — RTU that responds with status, alarm, temperature, power
