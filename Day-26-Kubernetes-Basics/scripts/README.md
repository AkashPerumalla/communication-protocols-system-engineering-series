# Script Notes

`run_all_tests.sh` performs the Day-26 validation flow:
- Maven tests
- Docker image build
- Kubernetes YAML validation
- Namespace deployment on Docker Desktop Kubernetes
- Resource verification
- Health and API marker checks
