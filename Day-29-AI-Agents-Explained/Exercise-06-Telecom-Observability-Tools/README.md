# Exercise 06 - Telecom Observability Tooling

## Objective

Use telemetry, alarm, device, and log-oriented agent requests to simulate NOC operations triage.

## Steps

1. Submit telemetry lookup task.
2. Submit alarm summary request.
3. Submit log search request.
4. Compare tool selections in trace outputs.

## Solution

Run `run.sh` to execute multiple `POST /agent/run` calls and inspect traces.

## Outcome

You validate telecom-specific tool routing and grounded diagnostics behavior.
