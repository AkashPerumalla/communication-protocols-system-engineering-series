# Screenshot Notes

Capture the following screenshots for Day-26:
- `kubectl get pods -n day26-platform`
- `kubectl get svc,ingress -n day26-platform`
- `/actuator/health` response through port-forward or NodePort
- `/api/dashboard` and `/api/deployments` output
- HPA and rollout status examples
