# Exercise 01 - Trap Basics

Generate a deterministic set of agent traps and inspect the OID, severity, description, and timestamp fields.

## Run
```bash
javac --release 17 -d out ../core/*.java TrapBasicsDemo.java
java -cp out TrapBasicsDemo
```

## Expected Marker
- `TRAP GENERATED`
