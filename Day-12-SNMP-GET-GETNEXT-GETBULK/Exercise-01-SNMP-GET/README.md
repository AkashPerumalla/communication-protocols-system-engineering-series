# Exercise 01 - SNMP GET

Retrieve a single managed object with GET and observe the request, response, and response time.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') SnmpGetSimulatorDemo.java
java -cp out SnmpGetSimulatorDemo
```
