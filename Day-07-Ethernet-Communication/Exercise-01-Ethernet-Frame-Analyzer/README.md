# Exercise 01 - Ethernet Frame Analyzer

Objective: Understand Ethernet frame structure by parsing and displaying frame fields.

Run:

```bash
./scripts/run-ex01.sh
```

Expected Output: Frame breakdown including preamble presence, destination MAC, source MAC, EtherType, payload length, CRC.
