# Exercise 03 - Trap Processing

Run the receive, validate, decode, store, and forward pipeline on a modem failure event.

## Run
```bash
javac --release 17 -d out ../core/*.java TrapProcessingDemo.java
java -cp out TrapProcessingDemo
```

## Expected Marker
- `TRAP PROCESSED`
