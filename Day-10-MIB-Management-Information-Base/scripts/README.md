# Scripts

- `run_all_tests.sh` compiles all Day-10 Java sources with Java 17.
- It runs every exercise, stores logs in `captures/`, and validates required marker strings.
- The script is the canonical regression check for the module.
