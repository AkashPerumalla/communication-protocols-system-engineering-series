# Exercise 04 - Wireshark Packet Lab

Objective: Provide packet summaries and parsing examples for common protocols: ARP, ICMP, TCP, UDP, HTTP, DNS.

Run:

```bash
./scripts/run-ex04.sh
```

This exercise outputs textual summaries useful to learn Wireshark display fields and filters.
