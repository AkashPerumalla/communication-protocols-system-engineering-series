# Scripts

Run full Day-20 validation:

```bash
bash scripts/run_all_tests.sh
```

The script builds the project, runs tests, starts the application, validates required markers, validates API workflows, and verifies WebSocket endpoint/topic configuration.
