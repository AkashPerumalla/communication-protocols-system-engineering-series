# Exercise 08 - SatCom Ground Network

Objective: Simulate a hub controller and remote terminals (RTs) for ground network telemetry and health checks. Commands: `GET_STATUS`, `GET_TEMP`, `GET_POWER`, `GET_ALARMS`.

Run:

```bash
./scripts/run-ex08.sh
```
