# Exercise 04: Realtime Dashboard

Objective: Build an operator-ready dashboard with fleet health and alarm visibility.

Steps:
1. Call `GET /api/dashboard`.
2. Validate totals, online/offline split, and alarm severity counts.
3. Inspect recent control command history and device availability percentage.

Expected marker: DASHBOARD UPDATED
