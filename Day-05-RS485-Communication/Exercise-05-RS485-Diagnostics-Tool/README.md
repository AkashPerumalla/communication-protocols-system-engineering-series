Exercise 05 — RS485 Diagnostics Tool

Objective
- Provide discovery, ping/latency tests, packet counts and timeouts for RS485 nodes (simulated).

Files
- `DiagnosticsMaster.java` — runs discovery and latency tests across specified ports
- `DiagnosticsNode.java` — a lightweight node that responds to `PING` and echo tests
