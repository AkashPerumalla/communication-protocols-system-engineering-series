# Exercise 04 - OID Discovery

Use GETNEXT to explore an unknown branch and inspect current OID, next OID, object name, and value.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') OidDiscoveryDemo.java
java -cp out OidDiscoveryDemo
```
