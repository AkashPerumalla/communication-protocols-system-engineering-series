# Exercise 03 - ARP Protocol Simulator

Objective: Simulate ARP request/reply flows and maintain a simple ARP table.

Run:

```bash
./scripts/run-ex03.sh
```

Expected: A "Who has 192.168.1.1? Tell 192.168.1.100" request followed by a reply and ARP table showing learned entries.
