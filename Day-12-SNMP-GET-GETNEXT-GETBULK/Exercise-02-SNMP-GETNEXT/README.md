# Exercise 02 - SNMP GETNEXT

Traverse the hierarchy step by step and see how lexicographic successor lookup walks a MIB tree.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') SnmpGetNextSimulatorDemo.java
java -cp out SnmpGetNextSimulatorDemo
```
