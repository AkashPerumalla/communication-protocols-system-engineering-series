# Exercise 08 - NOC Dashboard

Aggregate router, switch, modem, and hub health into one operational dashboard view.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') NocDashboardDemo.java
java -cp out NocDashboardDemo
```
