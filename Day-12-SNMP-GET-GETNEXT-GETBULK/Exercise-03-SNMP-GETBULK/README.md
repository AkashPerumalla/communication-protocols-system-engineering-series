# Exercise 03 - SNMP GETBULK

Retrieve multiple values in one request and compare the request reduction against repeated polling.

Run:
```bash
javac --release 17 -d out $(find ../core -name '*.java') SnmpGetBulkSimulatorDemo.java
java -cp out SnmpGetBulkSimulatorDemo
```
